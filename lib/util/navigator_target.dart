import 'package:phosphor_flutter/phosphor_flutter.dart';

class NavigatorTarget {
  final PhosphorIcon icon;
  final String label;

  const NavigatorTarget({required this.icon, required this.label});
}